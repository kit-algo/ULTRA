#pragma once

namespace Construct {

    enum IdTag {Id};
    enum SortTag {Sort};
    enum RandomTag {Random};
    enum InvertTag {Invert};

    enum XYTag {XY};
    enum LatLongTag {LatLong};

    enum CompleteTag {Complete};
}
